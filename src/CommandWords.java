/*
package com.company;
import com.company.Commandss;
import java.util.HashMap;
import java.util.*;
public class CommandWords {
    int commandvalue;
    private HashMap<Integer, Commandss> validCommands= new HashMap<>();  //validCommands = new HashMap<>();
  /* Constructor - initialise the command words.*/

  /* public CommandWords() {
      //  validCommands =new HashMap<>();
        for ( command commandvalue : Commandss.values()) {
            validCommands.put(command,commandvalue);
        } }

    public Commandss getCommands(int command)
    {
     Commandss commandvalue = validCommands.get(command);
     if(commandvalue!=null)
     {
         return commandvalue;
     }
     else
         return Commandss.Quit;
    }
}
*/